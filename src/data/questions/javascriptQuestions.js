export const javascriptQuestions = [
  {
    category: 'javascript',
    slug: 'closure',
    title: 'Что такое замыкание в JavaScript?',
    shortAnswer:
      'Замыкание — это способность функции запоминать переменные из внешней области видимости даже после того, как внешняя функция завершила выполнение.',
    fullAnswer: [
      'Замыкание возникает, когда функция использует переменные, объявленные вне её тела. Такая функция сохраняет доступ к своему лексическому окружению.',
      'Это значит, что внутренняя функция может продолжать обращаться к переменным внешней функции даже после того, как внешняя функция уже отработала.',
      'Замыкания часто используются для инкапсуляции данных, создания фабрик функций, обработчиков событий и сохранения состояния без глобальных переменных.',
    ],
    codeExample: `function createCounter() {
  let count = 0;

  return function increment() {
    count += 1;
    return count;
  };
}
d
const counter = createCounter();

console.log(counter()); // 1
console.log(counter()); // 2
console.log(counter()); // 3`,

    interviewAnswer:
      'Замыкание — это функция вместе с лексическим окружением, в котором она была создана. Благодаря этому функция может помнить и использовать переменные из внешней области видимости даже после завершения внешней функции.',
    pitfalls: [
      'Не стоит говорить, что замыкание — это просто вложенная функция.',
      'Важно упомянуть лексическое окружение.',
      'Хорошо привести пример со счётчиком или обработчиком событий.',
    ],
  },

  {
    category: 'javascript',
    slug: 'var-let-const',
    title: 'Чем отличаются var, let и const?',
    shortAnswer:
      'var имеет функциональную область видимости, let и const — блочную. const запрещает повторное присваивание переменной, но не делает объект неизменяемым.',
    fullAnswer: [
      'var был основным способом объявления переменных до ES6. У него функциональная область видимости, а не блочная.',
      'let и const появились в ES6. Они имеют блочную область видимости, поэтому переменная доступна только внутри блока, где она объявлена.',
      'const запрещает повторное присваивание самой переменной. Но если в const лежит объект или массив, их внутреннее содержимое всё равно можно изменять.',
    ],
    codeExample: `if (true) {
  var a = 1;
  let b = 2;
  const c = 3;
}

console.log(a); // 1
// console.log(b); // ReferenceError
// console.log(c); // ReferenceError

const user = { name: 'Mike' };
user.name = 'Mikhail'; // Можно

// user = {}; // Нельзя`,
    interviewAnswer:
      'var имеет функциональную область видимости и всплывает иначе, чем let и const. let и const имеют блочную область видимости. const не позволяет присвоить переменной новое значение, но не делает объект глубоко неизменяемым.',
    pitfalls: [
      'Не говорить, что const делает объект полностью неизменяемым.',
      'Важно упомянуть блочную область видимости let и const.',
      'Можно дополнительно сказать про temporal dead zone.',
    ],
  },

  {
    category: 'javascript',
    slug: 'event-loop',
    title: 'Что такое event loop в JavaScript?',
    shortAnswer:
      'Event loop — это механизм, который позволяет JavaScript выполнять асинхронный код, не блокируя основной поток.',
    fullAnswer: [
      'JavaScript в браузере обычно выполняется в одном основном потоке. Это значит, что синхронный код выполняется последовательно.',
      'Асинхронные операции, например таймеры, промисы или сетевые запросы, передаются браузерным API. Когда они завершаются, их callbacks попадают в очереди задач.',
      'Event loop следит за call stack и очередями задач. Когда call stack пуст, он берёт следующую задачу и передаёт её на выполнение.',
    ],
    codeExample: `console.log('start');

setTimeout(() => {
  console.log('timeout');
}, 0);

Promise.resolve().then(() => {
  console.log('promise');
});

console.log('end');

// start
// end
// promise
// timeout`,
    interviewAnswer:
      'Event loop — это механизм, который координирует выполнение синхронного и асинхронного кода. Сначала выполняется call stack, затем microtasks, например Promise.then, и после этого macrotasks, например setTimeout.',
    pitfalls: [
      'Важно не говорить, что JavaScript сам по себе многопоточный.',
      'Стоит различать microtasks и macrotasks.',
      'Promise.then выполнится раньше setTimeout с задержкой 0.',
    ],
  },

  {
    category: 'javascript',
    slug: 'promise',
    title: 'Что такое Promise?',
    shortAnswer:
      'Promise — это объект, представляющий результат асинхронной операции: успешное выполнение или ошибку.',
    fullAnswer: [
      'Promise используется для работы с асинхронным кодом. Он может находиться в одном из трёх состояний: pending, fulfilled или rejected.',
      'Метод then используется для обработки успешного результата, catch — для ошибки, finally — для кода, который должен выполниться в любом случае.',
      'Promise помогает избежать глубокой вложенности callbacks и делает асинхронный код более читаемым.',
    ],
    codeExample: `function loadUser() {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      resolve({ id: 1, name: 'Mike' });
    }, 1000);
  });
}

loadUser()
  .then((user) => {
    console.log(user.name);
  })
  .catch((error) => {
    console.error(error);
  });`,
    interviewAnswer:
      'Promise — это объект для работы с асинхронными операциями. Он сначала находится в состоянии pending, затем переходит либо в fulfilled с результатом, либо в rejected с ошибкой.',
    pitfalls: [
      'Не путать Promise с результатом операции.',
      'Promise сразу создаётся, а результат появится позже.',
      'Важно знать состояния pending, fulfilled и rejected.',
    ],
  },

  {
    category: 'javascript',
    slug: 'this',
    title: 'Как работает this в JavaScript?',
    shortAnswer:
      'this определяется не местом объявления функции, а способом её вызова. У стрелочных функций своего this нет.',
    fullAnswer: [
      'Значение this зависит от того, как вызвана функция. При вызове метода объекта this обычно указывает на этот объект.',
      'При обычном вызове функции в strict mode this будет undefined. В нестрогом режиме может указывать на глобальный объект.',
      'Стрелочная функция не создаёт собственный this. Она берёт this из внешней области видимости.',
    ],
    codeExample: `const user = {
  name: 'Mike',
  sayName() {
    console.log(this.name);
  },
};

user.sayName(); // Mike

const say = user.sayName;
say(); // undefined в strict mode

const admin = {
  name: 'Admin',
  sayName: () => {
    console.log(this.name);
  },
};

admin.sayName(); // this берётся не из admin`,
    interviewAnswer:
      'this в JavaScript зависит от способа вызова функции. Когда функция вызывается как метод объекта, this указывает на объект. У стрелочных функций собственного this нет, они используют this из внешнего лексического окружения.',
    pitfalls: [
      'Не говорить, что this всегда указывает на объект, где функция объявлена.',
      'Важно упомянуть отличие обычных и стрелочных функций.',
      'Можно привести пример потери this при передаче метода в переменную.',
    ],
  },
];